name: flutter_chatgpt_app
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.1.0
  http: ^1.2.1
  vibration: ^1.9.0
  rxdart: ^0.27.7
  flutter_local_notifications: ^15.0.0
  intl: ^0.19.0
  flutter_localizations: # Flutter SDK에 내장된 로컬라이제이션 지원
    sdk: flutter
  win32: 5.0.3 # 호환성 문제 해결을 위해 안정 버전 고정

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
